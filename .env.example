# Copy this file to .env and fill in real values.
# No API keys strictly required for MVP (pretrained local embeddings),
# but include this if you later add Semantic Scholar / arXiv API keys
# or an LLM API key for RAG extension.

SEMANTIC_SCHOLAR_API_KEY=
